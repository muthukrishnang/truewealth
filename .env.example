# Database (PostgreSQL). Local example with Docker:
# docker compose up -d  then use:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/truewealth"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""   # run: openssl rand -base64 32

# Google OAuth (create at https://console.cloud.google.com/ → APIs & Services → Credentials)
# Redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
